name: CI/CD Pipeline

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  test-backend:
    name: Build & Test Soroban Smart Contract
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install Rust Toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown

      - name: Run Cargo Test
        run: |
          cargo test
        working-directory: split_sync

  test-frontend:
    name: Build & Test Next.js Frontend
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install Dependencies
        run: npm install --ignore-scripts
        working-directory: frontend

      - name: Run Frontend Tests
        run: npm run test
        working-directory: frontend

      - name: Build Application
        run: npm run build
        working-directory: frontend

  deploy:
    name: Deploy Frontend & Contract
    needs: [test-backend, test-frontend]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/master' && github.event_name == 'push'
    env:
      HAS_VERCEL_SECRET: ${{ secrets.VERCEL_TOKEN != '' }}
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Deploy to Vercel (Production)
        if: env.HAS_VERCEL_SECRET == 'true'
        uses: amondnet/vercel-action@v20
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
          working-directory: frontend
          vercel-args: '--prod'

      - name: Log Deployment Action Skip (No Secret Configured)
        if: env.HAS_VERCEL_SECRET != 'true'
        run: echo "Vercel CD deployment step skipped because VERCEL_TOKEN secret is not configured in repository settings."
